## Применение оператора
Является вспомогательным оператором для циклов и для оператора выбора `switch`. Может использоваться во всех 3 видах циклов.

Оператор `break` в цикле *заставляет прервать* цикл и перейти к выполнению следующего оператора (следующего за циклом).

Оператор `continue` воздействует только на структуру (цикл), его содержащую (если циклов несколько, то он будет действовать на тот из них, в который он включен).

Пример:
```c
printf ("Введите результат первой игры (или q для завершения): ");
while (scanf ("%f", &score) == 1) {
	if (score < MIN || score > MAX) {
		printf ("%0.1f -- недопустимое значение. Повторите попытку: ", score);
		break;
	}
	printf ("Accepting %0.1f:\n", score);
	min = (score < min) ? score : min;
	max = (score > max) ? score : max;
	total += score;
	n++;
	printf ("Введите результат следующей игры (или q для завершения): ");
}
```
Здесь с помощью оператора `break` досрочно завершаем серию ввода результатов, если встретили `score` вне диапазона `0.0 - 100.0` (выполняется переход к следующему оператору за телом цикла).

![[Pasted image 20260213175118.png]]